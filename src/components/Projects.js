import { SectionHeading } from './SectionHeading';
import Cards from './Cards';

export const Projects = () => {
  return (
    <section id='projects'>&zwnj;
      <SectionHeading index={2} heading='Projects' />
      <Cards />
    </section>
  );
}